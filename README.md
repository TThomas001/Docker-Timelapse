# Docker-Timelapse

```
Docker-Timelapse
├── .dockerignore                       docker build ignort file list
├── Dockerfile                          docker build file
├── corntab                             corn job file
├── main.py                             timelapse processing python scirpt
├── requirement.txt                     require python packages
├── docker-compose.ymal                 contianer run config file
```
